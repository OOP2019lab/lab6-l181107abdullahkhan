#include"Header.h"
int main()
{

	/*student s1;
	student s2(3,2.0);
	s1.addquizscore(10);
	s1.addquizscore(5);
	s1.addquizscore(15);
	s1.print();
	s2.addquizscore(12);
	s2.addquizscore(10);
    s2.addquizscore(14);
	s2.print();
	student s3(s1);
	s3.print();
	s1.updatescr(1,3);
	if(s1.compare(s2)==true)
		cout<<"equal"<<endl;
	else
		cout<<"not equal"<<endl;
	s3.setgpa(3.0);
	s3.getgpa();
	// the copy constructor was invoked when s3 was initialized because another object was passed in as argument//
	//destructor is invoked when the compiler exits the main body and it functions in reverse order*/

	student s[3];
	




	return 0;
}